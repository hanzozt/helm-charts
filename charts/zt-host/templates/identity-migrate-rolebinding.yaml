apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "zt-host.fullname" . }}-identity-migrate
subjects:
  - kind: ServiceAccount
    name: default
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "zt-host.fullname" . }}-identity-migrate
  apiGroup: rbac.authorization.k8s.io
